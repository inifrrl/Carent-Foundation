
  # Build website with interactions (Copy) (Copy)

  This is a code bundle for Build website with interactions (Copy) (Copy). The original project is available at https://www.figma.com/design/b6rOgoxobn9dOY43RdoH0I/Build-website-with-interactions--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  